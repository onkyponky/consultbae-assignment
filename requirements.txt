# Phase 1 - ingestion.
#
# CSV reading uses the stdlib `csv` module deliberately, so pandas is not a
# dependency here. See CLAUDE.md for the reasoning.
SQLAlchemy==2.0.52

# Phase 2 - normalisation test suite.
pytest==9.1.1

# Phase 3 - audio collection app.
#
# Audio metadata comes from the ffprobe and ffmpeg binaries via subprocess,
# not from a Python audio library. pydub would only wrap the same calls and
# librosa would pull in numpy and numba to recompute what astats already
# reports. Neither is a dependency here.
fastapi==0.141.1
uvicorn==0.52.3
Jinja2==3.1.6
python-multipart==0.0.32

# Later phases will add nothing further; the n8n flow (Task 2) is exported
# JSON, not a Python package.