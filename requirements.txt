# Phase 1 - ingestion only.
# CSV reading uses the stdlib `csv` module deliberately: it surfaces
# structurally broken rows instead of silently coercing them.
SQLAlchemy>=2.0

# Later phases will add: web framework (Task 3), audio metadata
# extraction (Task 3). Not listed yet - nothing here is unused.